/// Shared geometry for the collapsed composer and its replacement bars.
class ChatInputBarMetrics {
  static const double inputHeight = 36;
  static const double verticalPadding = 5;
  static const double height = inputHeight + verticalPadding * 2;
}
