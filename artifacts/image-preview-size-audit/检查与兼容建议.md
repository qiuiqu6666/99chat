# 全屏图片预览尺寸审查

范围：当前工作区源码；未修改产品代码。GitNexus 当前没有可用仓库索引，使用源码核对，未把无图结果当作无调用。

## 验证

调用真实布局函数，覆盖 5 种窗口 × 9 种图片，共 45 组：320×568@2、390×844@3、844×390@3、1024×1366@2、1920×1080@1；图片包括小图、窄长图、分类阈值附近截图、十万像素长图、横向全景、48MP 照片及 1×100000 极端图。参见 size_matrix_test.dart 和 tests.log。

测试运行 27 项通过、1 个测试文件编译失败：旧 image_preview_resolution_utils_test.dart 引用已不存在的 panorama/isPanorama，不能宣称全部回归通过。尺寸矩阵是计算/逻辑验证，不是真机像素截图或内存峰值测量。

## 发现

1. 小图分类没有落实不同显示策略。resolution_utils.dart 的 small、normal、wide 最后同为 contain。实测 64×64 在 390×844 窗口显示为 390×390。源码“小图不放大”的注释与行为不符。窄长图先按比例分类，也没有原始宽度保护。
2. 长图宽度总是屏幕宽度，没有横屏或大窗口阅读列限制。1080×12000 在 844×390 窗口显示为 844×9377.78，在 1920×1080 窗口显示为 1920×21333.33。比例没有变形，但初始阅读密度差异明显。
3. 分块渲染的缩放未作用于显示几何。tile_geometry 的 scale 只参与 dstWidth；contentHeight、tileLogicalHeight 和 widget 中的显示宽度均不随 scale 改变。测试中 1x 与 4x 均为宽 390、高 4875；因此当前缩放主要增加像素而非放大内容。
4. 分块内存预算未在分配前约束单块和在途请求。8000×100000、390×844@3、4x 时，一块输出请求为约 189596160 字节（180.8 MiB）的 RGBA 位图；不是实测设备峰值。附近分块不在淘汰候选内，在途相同索引也没有 single-flight 合并；现有 12MP 手机缓存预算不能保证分配峰值。Android/iOS/Windows 目标宽高入口没有对应总像素硬上限。
5. 分块路由按任一边 >16384 或总像素 >40MP 选择，因而不仅用于超长图，也用于 30000×1000 全景和 8000×6000 普通照片；实际组件却是纵向 fitWidth 列表，缺少横向与二维放大浏览。相同形状的图片跨过解码阈值后，可能改变展示与手势策略。
6. 分块组件未见随视口尺寸/DPR 变化清理旧分块并维持阅读锚点的生命周期处理。主预览还按图片索引缓存 Hero 盒；窗口改变后的过渡位置需要组件级验证，当前未作实测结论。

## 建议的统一策略（待视觉验证的起点）

- 普通照片：contain，居中完整显示，允许用户缩放；不要为了消除黑边裁切或扭曲图片。
- 很小/低分辨率图片：单独限制初始放大，保留手动放大；不要套用长图铺宽规则。不能仅凭源像素和 DPR 推导唯一“自然大小”，应明确产品的初始显示上限。
- 手机竖屏清晰长截图：按可用宽度显示，从顶部阅读。
- 手机横屏、平板和桌面长图：阅读列居中，建议起点为 min(可用宽, 可用高, 720 逻辑像素)，放大仍可手势控制。720 是建议默认值，不是已验证的通用标准。
- 横向全景：默认全图概览，双击进入局部阅读，支持横向移动；极端长宽比仍需要概览与细读两种尺度，不能一屏同时保证所有细节可读。
- 超大图：展示策略与解码方式分离。普通解码和分块解码共享一套几何和缩放坐标。长图支持纵向、宽图支持横向、放大后支持二维平移。
- 清晰度：按显示逻辑尺寸×DPR×缩放倍数计算可见区域所需像素，并受源像素和分配预算限制。分块前固定目标块像素上限（可先评估每块 1024×1024 或 2048×2048）、总在途预算、并发限制和过期请求丢弃。缓存预算不能代替分配预算。
- 窗口/DPR 变化时重算布局并保留图像坐标中的阅读中心或顶部锚点，而不是沿用旧屏幕像素偏移。

实施顺序：先修分块几何与分配预算，再统一初始宽度/小图规则，最后补横屏、分屏、旋转、错误缩略图回退和 Hero 对齐的组件/真机验证。

Flutter 官方资料：https://api.flutter.dev/flutter/painting/BoxFit.html 与 https://api.flutter.dev/flutter/widgets/Image-class.html 。contain 允许放大；scaleDown 仅缩小；解码内存取决于像素尺寸而不是压缩文件大小。
