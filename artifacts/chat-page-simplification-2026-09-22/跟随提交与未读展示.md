# 跟随提交与未读展示收敛

## 改动

- 删除滚动处理末尾不可达的 `resumed` 成功分支。该位置前面的缓冲处理已完整返回，末尾连再次揭示调用也不需要；可见确认继续负责恢复跟随。
- `restoreTowardLatestFromUserScroll` 保留 deprecated 兼容 API，保持只揭示、不提交、永远返回 false 的原语义。
- 按钮提交通过 `commitFollowAfterVisibleLatestConfirm` 进入模型唯一的检查/快照核心，不再复制 visit、receive generation、restore op、covered IDs 的采集。
- `targetTipId` 原本不参与提交验证，改为可选 deprecated 参数保留既有调用兼容，生产不再计算 tip；实际的最新行物化/几何证明未删除。
- UI 的 type、显示数量、点击数量和自动 settle 统一从 `remainingLiveIncomingCount` 派生，selector 不再监听旧 `receivedCount`。数据库 ACK、visit、owner、代次、entry 提示策略保持原有职责。
- 删除底部类型不可能为 showPrevious 的两处转换。

## 图影响

刷新后以 CLI 重新执行 impact（MCP transport 已断）。提交/显示 helper 均 LOW；`_syncFollowingLatestFromUserScroll` 直接 4、总影响 12、1 个流程。旧 reveal API 为 UNKNOWN，补查唯一生产引用和测试引用。私有 `_TongueUnreadSelectorData` 为 HIGH、153 影响、2 个直接文件节点，已在修改前告知根代理并向用户提示；源码引用限定当前按钮文件。原始结果见 `impact-*.log`。

## 验证

- 第一阶段 before/after：live_follow_restore、history_incoming_position、durable_incoming_capsule_regression 共 47 项全部通过。
- 新增 chat_unread_presentation_state_test 共 4 项。前三项覆盖 scalar 与 ledger 不一致、读后 scalar 残留；第四项复现已在底部且 scalar=7/ledger=0 时 8 帧重复提交 FOLLOW 8 次，修改后为 0 次。
- 第二阶段最终：新 4 项 + 原 47 项，共 51 项全部通过，见 `unread-presentation-after.log`。
- 新测试 `dart analyze` 无问题，两个独占业务文件 `git diff --check` 通过。
- 两个旧证明测试文件 before 共 6 项失败，与之前 4+2 分类一致；本轮整体旧回归的最终比对由根代理统一执行。

未改未读数据库语义，未提交代码。消息列表其他区段由并行代理处理，本说明只对应末尾死分支修改。
