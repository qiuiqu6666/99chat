# 滚动分页调度收敛

最终复验：两个敏感场景恢复开工基线首败，新增终点探测用例通过；包含本文件所有相关场景的整合测试 **184/184 通过**，见 `integration-final.log`。以下保留分阶段验证和发现记录；最后两次兼容修复已纳入最终结果。

主体收敛完成，生产与测试文件冻结。旧敏感回归复核后追加的一次终点探测兼容修复，等待根代理统一复验。保持原 SDK Future、窗口代次、分页补偿、latest 调度和短视口 pointer 适配器的职责。

## 实际简化

- previousGestureRetry、loadPreviousDebounce 两个 timer 合为 ChatPreviousLoadQueue 的一个 timer；重复事件更新 intent，不延后首次 120ms deadline。
- 删除原散落的 previousGestureRetry、loadPreviousDebounce、_pendingLoadPreviousAnchor、pendingLoadPrevious 四个字段。pending/获准显示状态由队列统一维护，不再由 spinner、调度入口、失效清理分别写入。
- 同一 ScrollUpdate 按 edge 或 prefetch 投递一次，自动补屏、短触摸、滚轮、trim 恢复共享 _evaluatePreviousLoadIntent；_loadPrevious 保留唯一执行入口。
- 等待意图不占 trim 锁。有效用户意图不能被后续自动填充降级；过期用户 owner 必须先淘汰，不能吞掉新窗口自动填充。
- 过期 decision 只在无新 admitted/in-flight 时清旧 spinner，不对新窗口安排 trim。
- 零增长不会自动重试。短触摸的重试权限在形成意图时捕获，不能借用等待期间才出现的失败状态。
- 短视口 post-frame 自动填充补齐 model/conversation/window fence。

追加兼容修复前，与 before 快照比较：message list 净 -190 行，gate 净 +59 行；其中 list 的第一阶段死分支净 -18 行。本滚动阶段生产代码净 -113 行。不是把旧双调度包装进新类。追加修复后的最终净行数由根代理统一重算。

## 行为验证

开工基线：gate 15 + Community 13 + short viewport 11，共 39/39，见 scroll-scheduler-baseline.log。

追加终点探测修复前：原 39 项 + queue 新增 5 项通过（scroll-scheduler-serial.log 的 44 项）；新增真实混合输入用例独立通过（scroll-mixed-input-viewport-regression.log），共 45 项覆盖通过。追加修复后的根代理统一回归尚待完成，新增终点探测用例暂未执行。

queue 新增覆盖：保持首次 deadline、等待不占 trim 锁、用户提升不能被自动降级、过期 owner 不吞新请求、失败/取消不延续授权。

真实 widget 新增覆盖：短页失败后 pointer down → 缩小 viewport → wheel 发出一个受控未完成请求 → 恢复短 viewport → 原 pointer 才越过 slop → 请求零增长；请求数必须保持 2，全新 touch 才能发出第 3 次并加载成功。

收敛中发现并修正一个回归：model.isLoadingChatHistory 跨窗口包含旧 SDK key，不能加入所有 UI 来源的共享锁。最终沿用当前 UI task 和窗口 fence，不清理旧 SDK key，也不等待失效 owner 的 Future。

## 旧敏感失败位置变化的复核

同条件 before overlay 与 current 对比发现本轮另一个行为回归：初始 haveMoreData=false 时，旧常规 scroll scheduler 仍允许第一次明确上划执行一次终点探测。该事务同时设置 memorySuppressed=true，使后续阅读历史时的入站保持 durable 路径。新单一 edgeGesture 入口最初直接被 _canAttemptPreviousHistoryPagination 拒绝，导致入站改走 route_buffer；媒体恢复测试因此提前失败于 hasDurableHistoryDeferred，300 条入站测试提前失败于内存窗口达到 284 条而上限为 280。

证据：legacy-sensitive-before-overlay.log 第 34/36 行允许 no-more debounce，第 39/40 行建立 memory suppression；legacy-sensitive-after-current.log 第 32 行 queue_edgeGesture_discard，第 34/35 行 memorySuppressed=false 并进入 route_buffer。并非因为两个测试本来失败就可以忽略该新变化。

最小修复仅在共享 decision 中恢复 edgeGesture 且 !triedPreviousAfterNoMore 的一次探测例外，保留统一队列、零增长/terminal 消费位及其他来源的原限制，不重新引入双调度。fresh impact 为 LOW（直接 1/总 7/一个 tuiBuild 流程），见 scroll-impact-terminal-probe.json。

首轮兼容补丁复验还揭示第二个差异：edge 输入形成时 pixels/maxScrollExtent 为 936/936，等待 120ms 期间懒布局把 maxScrollExtent 扩到 5864，pixels 仍为 936。共享 decision 再次要求 edge band，丢弃了已经合格的边缘意图，旧 debounce 则继续执行。追加最小修复后，只有 shortTouch 重新校验短视口/边缘几何；edgeGesture 唯一生产来源 _rememberPreviousEdgeGesture 已在输入时检查 band。缺失 position、outOfRange 仍等待，owner/window/gesture fence 和真实反向取消仍保留。沿用同轮 LOW 影响依据，最终图由根代理刷新。

这两个懒布局敏感测试将验证已接受 edge 意图能否跨布局增长保持；新增 terminal fixture 保持稳定布局，不额外扩大测试装置。当前源码已冻结，补丁后的验证尚待跟随代理完成。

新增但尚未执行的测试：test/community_older_gesture_recovery_test.dart 中 `an initially exhausted edge admits one reading-window probe`，验证 idle 不探测、第一次真实 edge 仅请求一次并建立 reading-window suppression，随后 rebuild 和新拖动不重复 terminal probe。敏感两项、旧回归与新增行为验证由根代理和跟随代理统一安排；本代理未抢占 Flutter runner。

一次并行 Flutter 编译进程停止进展，已仅停止本次测试 session 并串行复跑；用户原有 flutter run 保留。定点 dart analyze 仍受 vendored 包反向引用 demo 诊断服务的既有 URI 问题影响，未作为 clean 分析声明；新增 queue/测试未出现分析诊断。git diff --check 通过。

## 影响分析

新图 pre-edit impact 归档为 scroll-impact-*.json。三个调度入口分别 LOW（_loadPrevious 直接 3/总 7；_scheduleLoadPrevious 直接 2；_rememberPreviousEdgeGesture 直接 1）。_clearTopHistoryLoading、cancelPreviousGestureRetry 为 MEDIUM。

Gate 类级 HIGH（直接 5/总 194），主 State 类级 CRITICAL（直接 5/总 509，含接口动态派发 lower-bound）；已通知根代理并向用户报告。生命周期和测试入口 UNKNOWN 均补查实际框架/fixture 引用，没有将零调用当作可删除证据。

未提交代码。最终索引刷新、detect_changes 与整体回归由根代理负责。
