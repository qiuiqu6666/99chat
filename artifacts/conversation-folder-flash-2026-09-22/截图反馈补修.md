# 顶部分组切走再回来才有会话：补修

用户截图为群聊页顶部自定义分组，已经显示业务空状态。上一轮仅处理请求进行中的空列表，没有把 SDK 成功响应中的缺失成员视为尚未解析，也没有在同步就绪时恢复当前分组补查。

本次新增真实 Conversation 群聊页测试。补修前请求次数期望 2、实际 1，明确失败于补查未恢复（group-sync-before.log）；补修后同一用例通过（group-sync-after.log）。这证明了一条符合反馈的代码路径，尚无用户设备日志用于断言它是现场唯一原因。

修改：

- SDK 返回 code=0 但请求成员仍缺失时，保留可重试状态，不将其认定为分组没有会话。真正 members 为空的分组仍正常显示空状态。
- 监听普通同步、等待服务器同步和首次同步完成的就绪变化，当前分组缺失成员时自动补查。
- 同步结束发生在补查返回之前时，使用就绪版本在旧请求完成后补查一次；不并行重复请求，不使用轮询。
- 仍不完整时保留手动重试入口，也可点击当前分组重试；已显示的行保留。
- 原空结果测试改为验证“成员存在但未查到”可重试；增加群聊页 4 种同步/返回先后顺序测试。

证据：followup-regression.log 为最终相关回归；followup-analyze.log 无 error，23 项现有 warning/info；git diff --check 通过。

GitNexus 绑定 99chat，查询索引时间 2026-09-21T22:09:07.322Z。刷新因 Windows 索引文件占用失败，见 reindex-followup.log，未宣称索引已刷新。编辑前 _hydrateFolderRows impact=CRITICAL（3 个直接调用、9 个上游节点；分组选择、成员更新、Feed 构建），已向用户说明。_onFeedSyncStateChanged 为 UNKNOWN，源码确认 addListener/removeListener；_buildFeedForSession 为 LOW。未提交代码。
