# 聊天页复杂度审查 — 2026-09-22

结论：存在可以明显简化的实现。主要来源是旧路径退役后留下的分支和状态、同一流程重复执行、同一业务含义由多处维护。以下区分确定冗余与需要分阶段调整的结构问题；本轮没有修改业务源码或测试。

本次核验当前工作树，包含上一轮四项缺陷修复。消息列表文件 15,594 行、global model 15,848 行、分页 runner 2,174 行、按钮容器 1,678 行；行数只描述范围，下面每项结论均有具体调用或状态证据，不以文件长为由建议重写。

## 建议先处理的确定冗余

### 1. 永远返回 false，却保留“恢复成功”的整段处理

[restoreTowardLatestFromUserScroll](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_live_window.dart:207>) 所有出口均返回 false，实际职责是按条件把缓冲消息加入显示窗口。[调用方](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:3623>) 仍根据 resumed 执行清插入空间、清锚点、释放 center、记录“恢复成功”等分支；这段成功分支不可达。

**更简单的实现：**把方法契约改成准确的“尝试揭示缓冲消息”，删除虚假的恢复成功返回值及死分支。实际恢复 FOLLOW 后的清理由既有可见确认流程负责。

**边界：**不能把 false 改成 true 来让分支执行。消息进入列表与用户已经看见消息，是两个不同阶段。沿用 live_follow_restore_test、history_incoming_position_test 的行为要求。

**影响：**helper 图结果 UNKNOWN，全文确认唯一生产调用及测试调用；调用方图 LOW，直接 4、总影响 12。是适合先做的小范围清理。

### 2. 回到底部的同一组提交前检查写了两份

[按钮容器的提交检查](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/TIMUIKitTongue/tim_uikit_chat_history_message_list_tongue_container.dart:295>) 与 [模型的提交检查](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_live_window.dart:316>) 都检查数据就绪、未接入缓冲，再读取 visit、接收代次、操作代次、coveredIds，最后调用同一个 commitLiveFollowRestore。两处还各自寻找 tip，但 [commit 方法](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_live_window.dart:222>) 的 targetTipId 参数未被方法体使用。

**更简单的实现：**几何与绘制证明仍由 UI 提供，模型提供唯一“可见确认后尝试恢复跟随”的入口，集中取快照、检查和提交。删除重复 wrapper；tip 参数若没有业务校验意义，就移除它及仅为它服务的计算。

**边界：**保留 owner/visit/op/receiveGeneration、未接入消息、可见证明和原子通知，不能压缩成“滚到底部就清零”。

**影响：**两个 wrapper 图均 LOW；UI wrapper 直接 4、总影响 9，模型 wrapper 直接 2、总影响 3。扩展方法调用有图漏边，已补源码核验。验证使用 live_follow_restore、durable_incoming_capsule_regression、return_to_latest_rendered_proof、chat_visible_latest_ack_recovery；后两组有旧失败基线，应比较新增回归。

### 3. 云端空页后，回退流程重复查询一次相同云页

[首次 CLOUD 请求](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_pagination_load.dart:751>) 为空后进入 [fallback](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_pagination_load.dart:949>)；[_unionOlderFetch](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_pagination_load.dart:165>) 又先查 CLOUD，之后才查 LOCAL。同一 attempt 的实际来源顺序是 CLOUD → CLOUD → LOCAL，重复请求的游标和页大小相同。IM06 只合并正在进行的请求，不能消掉串行重复。

本轮单跑 [SDK 本地回退用例](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/test/history_filtered_sdk_page_regression_test.dart:277>) 通过：它明确断言游标 [99,91,91,91]，后三次分别是 CLOUD(91)、CLOUD(91)、LOCAL(91)。日志见 logs/simplicity-pagination-local-fallback.log。

**更简单的实现：**回退函数复用已经得到的云响应，直接继续 LOCAL；若需要再次请求云端，把它表达成有次数和触发条件的重试策略，不要进入 fallback 就隐式从头再来。

**边界：**保留过滤前游标、LOCAL 群序号连续性、空与失败的区别、短页不能直接认定结束、5 秒节流和窗口有效性检查。

**影响：**_unionOlderFetch 图 LOW、直接调用方 1。Runner 为 UNKNOWN，源码确认 VM 经 controller 绑定进入。该项既降低流程复杂度，也直接减少一次无必要网络请求；尚未做真实网络耗时测量。

### 4. 已退役的归档分页仍留有只初始化、重置或打印的状态

[HistoryPaginationController](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/controllers/history_pagination_controller.dart:24>) 中 archiveOlderExhausted、archiveOlderActive、archiveNextToSeq、archiveOlderCursor、archiveSnapshotMaxSeq、archiveLastAcceptedOldestSeq、archiveLastAcceptedPage、windowReplay、archiveChecksumByRequest、archiveLoadingState、suppressArchiveUntilSdkHistory 等，当前工程内没有生产分页决策消费。部分只定义/重置；部分还有赋值与诊断输出。

**更简单的实现：**清理退役字段、无意义的赋值和日志参数，让 controller 仅表达当前 SDK/cache 分页的状态。

**边界：**[resetCommunityCursor](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/controllers/history_pagination_controller.dart:64>) 里的 cacheReadContinuations.clear() 仍有效，不能整段删除。archiveHistoryNotice 仍使用，公共 HistoryWindowReplayController 在别处的用途也不能一起删。若该 vendored package 需要兼容工程外调用者，删除公开字段之前还需明确其 API 范围。

**影响：**字段图 UNKNOWN，已全文核对 lib、vendor lib、test；共用 reset 图 HIGH，直接 4、总影响 12，涉及初始化、清空、搜索和回到最新。因此“删闲置字段”与“删整个 reset”风险完全不同。

### 5. 常量关闭的短历史顶对齐，仍带着整套测高和 spacer 逻辑

[功能开关](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/controllers/chat_list_route_scroll_restore.dart:8>) 是 const false。消息列表仍有旧的短列表测高、latch、spacer、短路径 reveal 分支。生产路径下，spacer 正值入口被该常量归零/挡住，latch 的正向入口又依赖 spacer 或直接受该开关保护，旧顶对齐链不可达。

**更简单的实现：**按当前“底对齐”的既定行为裁掉旧顶对齐链，首屏显示统一走活跃路径。继续保留不足一屏时自动补页和用户触摸重试，这与顶对齐是两件事。

**边界：**不能按 shortHistory 名称批量删除。[_onHeadMessageLaidOut](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:7447>) 仍维护有效行高/媒体稳定信息；动态图片高度和当前首屏几何条件必须保留。

本轮单跑 short_history_top_align_kill_switch_test：3 通过、3 失败。第一条仍要求开关为 true，与当前 false 明确冲突；另外两条是旧源码字符串断言失配，不等于两个新运行缺陷。开关相关图影响 MEDIUM、19 个符号，涉及 initState/tuiBuild；验证保留 chat_history_idle_open_reveal 和 history_short_viewport_touch_retry。

## 收益较大、需要分阶段收敛的实现

### 6. 同一分页结果在没有新异步间隔时被反复读取和合并

[第一次提交准备](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_pagination_load.dart:1524>) 在 lifecycle await 完成后读取当前窗口，先合 baseline，再合回调结果。[第二次提交准备](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/separate_models/tui_chat_history_pagination_load.dart:1612>) 在没有新的 await 的区间重复读取并执行同样两次合并。下一个 await 在事实过滤阶段，前面这次重复读取并没有覆盖新的异步时间段。

**更简单的实现：**保留 lifecycle 前的回调输入，完成回调后由一个提交准备函数读取当前窗口，合并 baseline/结果，携带已验证的 raw boundary，应用事实并检查 scope 后提交。连续性证据只解释一次并传下去，避免多个阶段各自理解同一页。

**边界：**跨 await 的 rebase 仍必要；不能只保留最早快照。需要同时核验同步副作用和删除事实 await 后的 fence，保留本轮刚修复的“整页过滤仍推进 raw cursor”。helper 图 LOW 不代表整条核心分页可以当低风险改造。

[旧结构测试](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/test/history_pagination_stable_window_contract_test.dart:22>) 要求源码同时含 stableCommitBase 和 stableLatestBeforeCommit，实际上把实现结构固定住。重构时应改为行为断言：回调等待期间收消息、删除、切会话、clearEpoch 变化，最后不丢新消息、不复活删除消息、不提交旧窗口。

### 7. 底部按钮同时消费两份“新消息数”，展示来源应统一

[旧计数使用](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/TIMUIKitTongue/tim_uikit_chat_history_message_list_tongue_container.dart:1315>)、[selector](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/TIMUIKitTongue/tim_uikit_chat_history_message_list_tongue_container.dart:1613>) 与新的 remainingLiveIncomingIds 同时参与按钮策略、显示和自动 settle。[内存接收](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/business_logic/view_models/tui_chat_global_model.dart:4751>) 更新两份；持久路径又以 SQL 数量减 visit baseline、加 pending/revealed、取 max 来维护旧 scalar。

**更简单的实现：**为 UI 派生一个明确的状态结果：进入页面时的未读提示、本次访问尚未看见的新消息数、回底按钮可见性。底部实时新消息数量统一从身份账本计算；旧 scalar 若仍为持久队列兼容服务，就留在数据层并准确命名，逐步迁移调用者。

**边界：**进入前未读、SQL 待确认记录、本次访问未看见的消息不是同一个数字，不能全部相加或合并。上一轮计数修复已经补上持久入站，本条是遗留结构问题，不是在宣称修复后依然必然漏计。

**影响：**receivedNewMessageCountFor 图 CRITICAL，直接 7、总影响 53、8 个流程，应分阶段迁移，不适合全局替换。现存 receivedCount 残留断言说明维护两套口径的成本，但不能据此把 SQL ACK 当作“用户看见”。

### 8. 加载一页历史有多个 UI 调度入口和分散的状态所有权

[手势重试](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:9284>) 自己持有 120ms 重试 Timer 并调用 _loadPrevious；[常规调度](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:9405>) 又有 debounce、pending anchor 和一套 gate；[执行入口](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:9806>) 再检查保护状态；trim 恢复也可直接进入。UI gate 暴露大量可写字段，widget 又负责设置、失效和释放，条件组合分散在几个入口。

**更简单的实现：**保留触摸、滚轮、自动补屏、trim 恢复这些输入适配器，但把它们转成带来源与会话/窗口代次的同一种“加载意图”。一个调度器维护 pending/in-flight；同一个纯决策函数返回“允许、等待到某时刻、必须等新手势、丢弃”。入队与真正发出请求时复用该判定。

**边界：**不能把 UI 请求状态、数据请求状态、视口补偿三个生命周期合成一个 loading 布尔值。跨 await 必须重新检查；零增长不能自动死循环；反向/取消/禁用滚动/切会话必须丢弃旧触摸。自动补屏与真实用户重试也不能失去来源差异。

**影响：**_loadPrevious 图 LOW（直接 3、总影响 7），_scheduleLoadPrevious LOW（直接 2），_rememberPreviousEdgeGesture LOW（直接 1）；但图不表达时序与 Flutter 手势竞争，整体调整按中等行为风险对待。优先保留 community_older_gesture_recovery、history_short_viewport_touch_retry、chat_list_pagination_ui_gate 的现有行为测试。

## 其他可局部改进的点

- **分区缓存的八字段发布清单重复三次。** [第一处](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:12205>)、[第二处](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:12263>)、[第三处](<C:/Users/ASUS/Downloads/Telegram Desktop/99999999/third_party/tencent_cloud_chat_uikit/lib/ui/views/TIMUIKitChat/TIMUIKItMessageList/tim_uikit_chat_history_message_list.dart:12301>) 分别写 restoreVersion、listLen、headKey、unreadCount 等同一组字段。可引入一个不可变 cache key/snapshot 并一次发布，保留 prefix 快路径及索引映射。当前未发现字段遗漏；这是减少同步维护点，不是已复现运行错误，也不需要新增复杂控制器框架。
- **首屏 reveal 重建同一计时器，并在两处执行同一个硬超时。** 消息列表 1448 行的 schedule 每次先调用 1042 行 arm；1080 行取消并重建 100ms 周期 watchdog。1500ms 硬截止在 timer 和 1625 行 post-frame 分支分别执行。可让每个 reveal epoch 只 arm 一次硬超时，几何逐帧检查合并调度，通过一个 ready 提交入口完成显示。仍保留 400/1200ms 几何软预算、加载等待、1500ms 防永久透明截止，以及无后续帧时主动重绘、离页取消、已显示页面不重新隐藏。不能把语义不同的时间限制全部压成一个 timeout。图影响 LOW、8 个符号；chat_history_idle_open_reveal_test 的无输入揭示、停帧重绘、离页取消，以及 chat_initial_window_reveal_policy_test 是关键验证。这一项能减少实际重复调度，优先于单纯把缓存字段打包。

## 不应为了“简单”删掉的逻辑

1. owner/会话/窗口/clearEpoch 代次检查，承担账号切换、清空、异步晚回的隔离。
2. SDK 原始边界与过滤后显示列表必须分别保留，否则会重新引入整页删除后卡页。
3. 数据已接入、已绘制可见、已确认、恢复跟随是不同阶段。
4. trim 的持久提交和像素恢复，与截图遮罩的保留期限是不同生命周期；150ms 截图释放不能替代数据库完成条件。
5. global 权威消息、当前已绘制消息、菜单冻结消息具有不同帧/交互所有权，不能一律合并成同一 List。
6. 动态图片高度、键盘变化、前后台停帧、滚动方向、短列表触摸的真实差异仍要处理。

## 实施顺序与检查范围

建议先做小范围的确定冗余：死成功分支、重复回底提交入口、空云页重复请求；随后清退停用功能及闲置字段。分页提交、UI 调度、未读口径和 reveal 状态应分别迁移，每步保留上一轮四个缺陷的原始 9 项复现和新增 38 项回归，避免一次重写整个聊天页。

本轮是结构审查，仅定点运行上述两个现有测试文件/用例，未重复运行全项目测试。源码字符串测试可保留真正的架构边界，但不应依赖局部变量名、两次合并结构或已经失效的功能开关。

GitNexus 绑定 99chat，工作目录为 C:/Users/ASUS/Downloads/Telegram Desktop/99999999/，HEAD b6dcaaae0999d698f822222a393d59823d3ed55f。已刷新图索引至 2026-09-22 03:34:23（台北时间），incompleteReasons 为空；结束前状态仅显示独立的 qr_code_page.dart 有后续变动，聊天相关源码未漂移。FTS 不可用，概念查询为空后使用符号 context/impact 与当前源码交叉核验；流程枚举和 Dart callback/extension 图覆盖有限，UNKNOWN 均补查引用，不将空调用集认定为安全。
