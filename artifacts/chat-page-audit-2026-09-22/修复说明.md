# 聊天页四项缺陷修复 — 2026-09-22

本次修复对应同目录《审查报告.md》确认的四条问题。未提交代码；保留工作区原有改动。以下验证采用真实状态模型、SQLite 和 Flutter 组件，以及可控的 SDK 响应，未连接真实腾讯 IM 账号或 Android/iOS 真机。

## 修复行为

1. **历史浏览及后台入站的新消息提示**：持久入站与普通入站共用按消息 ID 去重的当前访问计数，并更新接收代次。访问切换、重复投递和删除不会重复增加提示；数据库 ACK 与用户实际看见消息的计数消费分开。
2. **SDK 整页过滤后的历史游标**：保存已通过校验、且成功提交到当前窗口的原始 SDK 分页边界。即使该页所有消息均被删除规则或生命周期过滤，下一次用户拖动仍能请求更早一页。保留重复页、错误方向、失效窗口检查，以及群聊原始页连续性校验；覆盖单聊同秒消息，不用发送方 seq 判断其先后。
3. **锁屏恢复后的清空版本同步**：区分数据库暂不可读与成功读取版本 0。仅在当前会话代次成功读取并采纳版本后标记完成；失败允许恢复后重试，旧账号的晚到结果不能覆盖新账号。
4. **不足一屏时触摸重试**：自动补页失败后，向旧记录方向的明确触摸拖动可通过既有分页锁和冷却队列重试。点击、小幅移动、反向、取消不会触发；同一手势不会不断请求。重新检查菜单、媒体和完整 physics 父链的滚动限制；切换会话、销毁及分页失效清除旧触摸。

## 测试记录

新增四个测试文件，共 **38 项全部通过**：

| 测试文件 | 通过数 | 重点 |
| --- | ---: | --- |
| `test/durable_incoming_capsule_regression_test.dart` | 9 | 前后台接收、混合路径去重、SQL ACK 与可见性、跨访问晚到重放、删除、实际按钮回底 |
| `test/history_filtered_sdk_page_regression_test.dart` | 15 | Public/Community、整页及部分删除、长删除跨度、生命周期过滤、同秒单聊、SDK 本地回退、重复/断层/过期响应 |
| `test/history_clear_epoch_retry_test.dart` | 3 | 后台禁止开库、仓库暂未挂载、旧账号晚到结果 |
| `test/history_short_viewport_touch_retry_test.dart` | 11 | 默认/AlwaysScrollable、限流、反向取消、菜单、禁用滚动、同 State 换会话、销毁 |

原审查保留的四份复现文件 **9 项全部通过**，日志：`logs/chat-fix-original-repros.log`。短列表原请求次数始终为 1，现拖动后为 2，成功显示 22 条消息。

相关回归（分组含上述专项测试，不相加作为独立总数）：

- 分页专项 + 历史窗口集成 + SDK 滚动连续性：48/48，`logs/chat-fix-pagination-final.log`。
- 清空版本重试 + 原同步测试 + 前台恢复契约：8/8，`logs/chat-fix-resume-final.log`。
- 新消息专项 + 跟随恢复 + remaining 计数：19/19。
- 短列表专项 + Community 长列表手势：24/24，`short-viewport-review-test-after.log`。
- 11 个既有历史、空闲显示、窗口裁剪/过渡和恢复文件：首次 107 通过、1 超时；该超时用例随分页最终组完整重跑通过（`history_window_pagination_integration_test.dart`）。初次记录见 `logs/chat-fix-history-suite.log`。

静态检查：四份新增测试通过 Dart analyze；四个业务文件的定点检查在第三方 package 自身分析上下文中无法解析原有的 demo 跨包 import，报告伴生未定义符号及原有 lint，不能视为静态全绿。真实 Flutter 测试使用主工程 package 配置编译并通过。日志：`logs/chat-fix-analyze.log`。本次业务文件的 `git diff --check` 通过。

旧未读计数相关五个文件最终为 **23 通过、38 失败**（`logs/chat-fix-legacy-final.log`），不能报告全项目或全部旧测试通过。其中原审查的四文件失败由 38 降为 36：`durable_unread_tongue` 17、`chat_visible_latest_ack_recovery` 2、`return_to_latest_rendered_proof` 4、`history_durable_scroll_to_latest` 13。另纳入的 `unread_visit_baseline` 有 2 条旧 `receivedCount` 标量断言失败；其 SQL 总数断言已通过，残留来自既有 `_publishHistoryDeferredState` 取最大值逻辑。未为了这些断言放宽可见确认或改动旧账本语义。本次中途发现并撤销了“SQL ACK 直接清当前提示”的不当耦合，`live_follow_restore` 最终 8 项通过。

图检查：所有改动前执行 impact；曾出现原生增量索引崩溃，随后完整重建及最终增量刷新成功。最终 runner 身份 current、`incompleteReasons=[]`，源码与索引一致（随后仅修复说明等记录文件更新）。`detect_changes(scope:all)` 返回 31 个已修改文件、132 个符号、2 条流程、medium，`partial=false`、`truncated=false`，见 `logs/fix-detect-changes.json`。这是整个已有脏工作树的结果，包含其他任务改动；Dart 回调/扩展及流程枚举仍有静态图覆盖限制，不能把仅两条流程理解为全部实际影响。本次风险判断同时依据源码、定点测试和独立审查。原本不可用的 FTS 仍禁用，图索引可用。

## 验证边界

本次修复解决四条已确认触发链，不能据此宣称所有真机锁屏组合或整个消息区持续空白问题均已消除。原审查未复现整页持续空白；本次同时回归初始显示、窗口裁剪、恢复和历史翻页行为。
