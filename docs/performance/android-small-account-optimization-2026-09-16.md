# Android 日常性能优化 — 2026-09-16

对应同目录 `android-small-account-audit-2026-09-16.md` 中用户选定的七类问题。
本次修改的是源码，没有覆盖手机上安装的应用，也没有发送消息或执行钱包交易。

## 已实施

| 路径 | 修改后的行为 |
| --- | --- |
| Android 系统通知 | 立即显示默认头像通知，后台串行补头像；取消了主线程 `Future.get(12s)`。队列最多 32 项，头像缓存最多 48 张，下载最多 2MB，采样到最长边 256。补头像使用静默更新，并核对通知实例 token，避免旧头像覆盖新通知或恢复已取消通知。 |
| 群成员单条更新 | `patchUser` 按 owner/group/user 主键读取目标记录。批量角色更新也只查询本次接受修改的用户。其他字段与不存在成员的行为保持原样。 |
| 相册扫码 | Android 解码、旋转、压缩改为串行后台 TaskQueue，采样阶段就限制最长边 4096；Dart 灰度增强在 compute isolate 执行，可选增强拒绝超过 4096² 像素的原图。保留二维码多次扫描与兜底，超时后迟到的临时图片会清理。 |
| 启动图 | 下载后在 isolate 完整验证并保存“图片内容 MD5 + 配置”标记；下次启动相同内容不再重复解码。旧缓存或内容变动重新验证；先检查文件大小与图片头尺寸，损坏图片回退。 |
| 群直播 | 相同群的并发 REST 请求合并；相同可见状态不通知聊天页。退出、切群或实时事件使旧请求失效。活动直播仍保留 12 秒校对，空闲群改为约 60 秒；后台暂停轮询，实时事件仍立即触发更新。 |
| 钱包群选人、主播 | 红包/群转账先打开页面、每次取 100 人，继续浏览才加载下一页。搜索优先云搜索，完整本地快照使用有上限的 SQL 搜索，其他情况保留逐页查找入口。本人过滤、隐私隐藏 ID、公开昵称和备注区分仍保留。在线状态只补可见成员。主播回显按 ID 查一人。旧钱包成员接口只返回 100 人预览；成员缓存 30 秒、最多 8 组且每组最多 200 人，并合并并发请求、校验账号。 |
| 在线状态、账单 | 在线状态 200ms 合并写盘、串行持久化，缓存每类最多 2048 条；清理账号时取消待写数据。批量响应包含隐私字段时不再逐人查询，缺失且调用方需要时才补查。缓存写盘不延迟 UI。账单改为 ListView.builder，仅为可见记录构建卡片并补昵称/红包发送者；后台详情队列最多 40 项、每批最多 20 条，退出停止续查。 |

## 验证

- 新增 `android_performance_optimizations_test.dart`：9 项，覆盖合并写盘、账号隔离、容量上限、分页/重试/旧结果丢弃、直播去重/状态变化/切群与清理、启动图缓存内容校验。
- 新增 `presence_batch_privacy_performance_test.dart`：3 项。100 人且批量隐私完整时只请求一次 `/presence/last-seen`；隐私缺失才调用单人接口；账号状态清理后旧响应不能写回。
- 回归覆盖 16 个测试文件，共 67 项，包括群角色、成员投影、实时游标、二维码夹具、钱包成员公开名称、钱包时间、直播和启动图。
- Android `:app:compileDebugKotlin` 编译成功，包含本次通知插件和扫码 TaskQueue 改动；依赖库仍有弃用提示。
- 改动核心文件的 Dart 分析无 error/warning；账单/主播页面保留原有 `intl` 间接依赖、重复 import 的 info 提示。较大的 `chat.dart` 还有本次未修改路径的既有 warning。
- 回归过程中共享工作区的另一个文件出现过瞬时 import 位置错误，恢复后已重新验证。播放横幅轮询改为单次 Timer 后，旧源码契约只接受 Timer.periodic；测试现在兼容两种 4 秒计时方式，仍禁止 8 秒退化。

## 效果证据与边界

- 6000 人合成群，桌面 Flutter + SQLite FFI：单成员更新原中位数约 15.1ms，修改后多轮约 3.7–4.0ms。目标行修改、相邻行不变、字段保留、缺失成员不创建均有断言。
- 原相同的两次“没有直播”响应会通知两次，现在通知 0 次；直播实际变化仍通知。
- 40 次同一缓存键的突发更新共享一次写入 Future，结果保留所有更新；6000 条输入持久缓存收敛到 2048 条。
- 原始数据库样本在 `test_outputs/android-small-account-audit-sqlite.json`。这些是主机测试，不代表 Android 帧耗时或真机提速百分比。
- 尚未安装这批代码进行 Android profile/release 真机对照。需要以同一账号、相同网络和场景验证通知突发、扫码大图、冷启动、群选人以及长账单滚动；不能据本次测试承诺全部场景无掉帧。
- GitNexus 用于修改前影响分析。动态 Dart 调用出现 UNKNOWN 时已核对实际调用点；源码所在目录 Git 元数据不完整，本次没有创建提交，也没有声称提交级图差异检查通过。
