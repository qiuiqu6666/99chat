# 三公聊天状态条更新滞后

## 定位

截图对应 GroupGameStatusBanner。每门金额与“共 N 注”来自
SangongAdminRealtimeState.toGroupGameRoundStatus，经过聊天页
_applySangongRealtimeState、ChatTopFixStateController 通知状态条。
控制器已比较 totalBetCount 和各门数组；数据到达后能够局部刷新。

已确认的缺口：

- 聊天页未持有三公订阅时也调用共享服务 release。其他聊天页关闭或
  状态刷新，可能错误释放仍在使用的游戏连接。
- SSE 断线重连只连 stream，没有重新读取 snapshot，断线期间的变化
  没有可靠补偿；静默连接没有兜底，切回前台也不主动恢复。
- HTTP 快照与 SSE 并行时，晚到的旧快照会覆盖新状态。
- 切租户/释放发生在启动等待期间时，旧 _starting 状态阻止新连接启动，
  旧异步结果也缺少生命周期、租户及账号校验。
- 重连等待旧流 cancel 完成，可能被空闲 HTTP 流的清理阻塞。

退出重进会重新启动服务并拉快照，因此能暂时恢复数据。

## 改动

- 页面仅释放自身实际持有的订阅，重复释放为空操作。
- 每次启动/重连并行获取快照与建立 SSE；慢快照不阻塞推送。
- 前台有订阅且 15 秒没有状态更新时补拉快照；正常状态推送会延后该请求。
- 45 秒无流数据时更换连接；建立流响应设置 20 秒上限。
- 后台停止连接与定时器，恢复前台立即重新拉取并订阅。
- 快照请求合并；晚于新推送返回的未知/相同/旧版本快照不再发布。
- 旧账号、旧租户、旧生命周期和旧连接的结果不再更新状态。
- 重连清空残留 SSE 解析片段；旧连接清理不阻塞新连接。
- 所有计数仍取服务端快照/推送，没有客户端推算下注金额。

## 验证

新增 test/sangong_realtime_recovery_test.dart 的 12 项测试，使用拦截的
HTTP/SSE 响应和虚拟时间。包括真实挂载状态条时，文本由“共100注”更新为
后续金额，无需销毁页面；以及断线、静默、后台返回、并发请求合并、切租户、
释放后重新订阅、多订阅、慢快照伴随连接失败、解析残片和空闲连接替换。

修改前四项核心场景分别停留在 100/100/200/100，期望值为
250/350/500/450，稳定复现缺失更新与旧快照覆盖。

新增测试连同 sangong_admin_realtime_test.dart、
chat_top_fix_state_controller_test.dart 共 19 项通过。
服务和新测试的 Dart 静态分析无问题；chat.dart 分析无错误，存在未改动
位置的既有告警/建议。本次没有进行 Android 真机弱网抓包或发布安装包。

影响检查使用 GitNexus 仓库 99999999 的 2026-09-16 索引，并核对当前源码。
服务修改为 LOW；聊天页订阅入口为 HIGH，涉及初始化与配置刷新，修改前
已说明风险。dispose/acquire/release 的图调用不完整，另外检查了全部实际
引用与释放点。本次没有提交 Git commit。
