**好友增删一致性与实时同步：前后端协议建议**

日期：2026-09-23。客户端依据：`725e6bc`。状态：待实现、待联调；本文件不是后端现状说明。

当前没有可访问的 99chat-server 源码或仓库，因此尚未验证服务端事务、版本分配、事件持久化和接口返回内容。以下字段与行为标为“新增”的均需要后端实现。现有客户端协议已经包含 snapshot、changes、itemVersion、eventId 和删除标记，可在此基础上扩展。

**1. 达到什么效果**

- 后端提交的好友关系是唯一权威；本地 SQLite 是它的可恢复副本。
- 本机操作成功后，用接口返回的已提交变更立即更新 SQLite，再通知页面；不依赖自己再收到一次推送。
- 其他在线设备通过 TCP 推送及时更新；漏推送、断线、崩溃通过增量补拉恢复。
- 离线期间展示最后确认状态和明确的待确认操作；恢复连接后收敛。不承诺离线设备与服务器同时变化。
- 旧事件、重复事件、迟到查询不能撤销更新的删除或重新添加。
- 建议在线同步体验目标为正常网络下 P95 小于 1 秒；这是待压测的目标，不是当前保证。

**2. 明确数据职责**

| 区域 | 职责 | 允许改变好友名单吗 |
| --- | --- | --- |
| 后端好友关系 | 判断增删、单向关系、双方权限，生成版本和变更日志 | 是，最终权威 |
| 本地 contacts.db | 保存已确认名单、版本、删除标记和同步进度 | 仅通过统一版本写入入口 |
| 本地待确认操作 | 保存 mutationId、操作类型、状态及 UI 临时覆盖 | 只影响待确认展示，不伪造服务端版本 |
| IM SDK 关系目录 | 后端关系在 SDK 中的投影、消息能力所需目录 | 回调只触发后端校验，不能直接改本地权威名单 |
| relation / 聊天权限缓存 | 保存 canMessage 等权限判断 | 无版本结果不能新增、删除好友行 |
| 用户资料库、名字和头像缓存 | 显示资料；删好友后可以保留资料 | 否；有资料不等于好友 |
| “已成为好友”聊天消息 | 展示提示、触发同步 | 否；延迟送达不能重新加人 |

好友列表以 `inMyFriendList` 的已确认状态为准，不用 `canMessage || isFriend` 代替。单向删除是否影响对方的名单、发送权限，由后端现有业务规则决定，客户端不得猜测。双方受影响时，后端分别产生各自视角的变更；备注等私有字段不能广播给对方。

**3. 后端事务与可靠通知**

每次真正的关系变更，在同一个数据库事务中完成：

1. 校验登录身份、操作权限和幂等键。
2. 更新好友关系，保留删除后的版本历史。
3. 分配该账号 contacts 域的递增 `contactSeq`，并生成递增 `itemVersion`。
4. 写入可补拉的变更日志与待投递事件（可复用同一张持久化日志，分别记录投递状态）。
5. 保存幂等操作结果，提交事务后返回客户端。

通知进程只投递已经提交的事件，失败重试。发送成功后进程崩溃可能导致重复投递，客户端必须幂等。不要在事务提交前推送，也不要只依赖一次进程内回调。

这采用事务发件箱模式，解决“数据库已更新但通知未发出”的双写缺口；不要求引入特定云服务或消息队列。参考：[AWS 事务发件箱说明](https://docs.aws.amazon.com/prescriptive-guidance/latest/cloud-design-patterns/transactional-outbox.html)。

同一账号的序号分配与提交顺序必须一致，不能把提前分配、尚未提交的序号当成可补拉水位。涉及两个账号的事务应按固定账号顺序锁定，避免互相等待。

后端到 IM SDK 的好友增删也应进入可重试投递流程。后端已经提交而 SDK 暂时失败时，记录待同步状态并重试，不把 SDK 的旧名单反向导入业务好友表。

**4. 三条传输路径使用同一份变更**

新增统一变更字段，保留现有 `SyncChangeEvent` 字段的含义。下面的数字和 ID 仅为协议示例：

```json
{
  "contactsEventSchema": 1,
  "ownerUserId": "owner-1",
  "clientMutationId": "operation-uuid",
  "events": [
    {
      "eventId": "contact-event-uuid",
      "contactSeq": 102,
      "id": "peer-1",
      "itemVersion": 18,
      "operation": "delete",
      "deleted": true,
      "occurredAt": 1790000000000,
      "data": {}
    }
  ],
  "contactsRevision": "opaque-server-revision"
}
```

- `eventId`：同一已提交变更在 HTTP、TCP、changes 三个通道保持相同；用于去重。
- `contactSeq`（新增）：当前 owner 的 contacts 域变更顺序；不能与其他业务域或历史协议的 seq 未经验证地混用。
- `itemVersion`：当前 owner + peer 的关系版本。删除也递增，重新添加继续递增，永不从零开始。可由统一域序号生成，但全链路必须使用同一规则。
- `contactsRevision`：继续作为服务器定义的不透明补拉令牌；客户端不把字符串字典序当作版本顺序。
- `clientMutationId`（新增）：重试同一个操作时保持不变。按登录账号和操作作用域去重；同键不同请求体应报冲突。相同键重试返回原已提交结果，不能再次修改关系。
- 序号和版本必须是精确正整数，协议范围不超过 JavaScript 安全整数上限；若后端需要更大范围，必须整链改用十进制字符串和精确整数比较，不能混用浮点数。
- upsert 建议携带完整的该账号视角好友记录，包括关系标志、已知备注及公开资料。发送好友申请仍为 pending 时，不生成“已成友”变更。
- 返回给操作人的 HTTP 响应仅包含其有权读取的投影；另一方通过自己的推送和 changes 获取自己的投影。

需要接入的现有接口：

| 接口 | 修改要求 |
| --- | --- |
| `POST /friend-requests` | 自动同意、恢复好友时返回已提交变更；普通 pending 保留申请结果 |
| `POST /friend-requests/{id}/accept` | 返回已提交好友变更，不只返回成功标识 |
| `POST /friend-application/accept` | 兼容入口必须走相同事务和变更生成逻辑 |
| `DELETE /me/friends/{peer}` | 返回带版本的删除变更；重复操作按幂等规则处理 |
| `PUT /me/friends/{peer}/remark` | 通过同一关系版本入口更新；空字符串明确表示清空 |
| `GET /me/friends/{peer}/relation` | 权限查询保持只读；若提供可入库关系，必须带相同版本协议，不允许无版本补写名单 |
| TCP 好友关系通知 | 携带与 HTTP、changes 同源的事件；旧通知只作为同步提示 |

**5. 客户端只保留一个已确认关系写入入口**

扩展现有 `applyProtocolChange()`，让 HTTP 操作结果、TCP 事件和 changes 都调用它。按账号串行协调同步与快照切换，并校验账号 generation，退出登录后的旧响应不得落入新会话。

一次 SQLite 事务内：

1. 校验 owner、字段和协议版本。
2. 读取好友行版本及删除标记版本，取较大者。
3. 入站 `itemVersion` 小于或等于当前版本则忽略；不能继续用被拒绝的 payload 刷新名字、头像或 UI。
4. 更高版本 upsert：更新好友行和搜索索引，清除较旧删除标记。
5. 更高版本 delete：删除好友行和搜索索引，保存带版本及 `contactSeq` 的删除标记。
6. 同事务保存去重信息，返回 applied / ignored / invalid 及实际接受后的状态。
7. 事务提交后，从接受后的状态派生目录和页面数据，合并发送一次通知。进程若在通知前崩溃，下次从库恢复。

例如：删除 v18 之后到达新增 v17，必须忽略；随后真正重新添加 v19，必须接受。不能通过临时 `force: true` 或客户端自行 `version + 1` 绕过权威版本。

请求尚未确认时的 UI 乐观状态，放在独立 pending 操作中。超时表示结果未知，使用原 mutationId 查询/重试，不立即认定服务端失败。明确失败只撤销对应 pending 状态，不把操作前整条旧记录回写数据库。同一个 peer 的本机操作应顺序提交，避免“删除后添加”的网络到达次序反转。

旧 relation 请求须同时检查账号 generation 和该 peer 的本地操作 generation；操作前启动的请求不能在操作后写权限缓存。更重要的是移除其无版本新增好友行的能力。

**6. 实时推送与补拉如何配合**

- 本机接口成功：立即应用返回事件，随后合并触发一次 changes 补拉。
- TCP 版本事件：立即走统一写入，再触发补拉；旧版无版本通知只触发补拉。
- SDK 好友增删回调、成友提示消息：只触发校验/补拉，不直接写好友关系。
- 登录、TCP 认证成功、网络恢复、应用回到前台：触发补拉。
- 心跳增加 contacts 最新水位检查，发现有未同步变更时补拉。这样最后一条推送丢失、之后再无事件，也不会永久停在旧状态。没有心跳水位能力时，可暂用前台低频增量校验，间隔按流量与延迟目标设置。
- 失败采用退避和抖动重试；认证失败等待重新登录，不能无限高频重试。

`ContactsProtocolSyncService.sync()` 需增加 dirty / rerun 标记：运行中又收到新触发，当前轮结束后至少再检查一轮，不能仅返回正在执行的旧 Future 而吞掉新需求。

游标规则：HTTP/TCP 的单个事件即使立即应用，也**不直接推进完整增量同步游标**。游标只由连续、完整处理的 changes 分页推进，避免收到第 102 条就跳过尚未收到的 101 条。好友行/删除标记、已处理分页和可恢复的分页游标应原子提交，崩溃后允许重复读、不允许跳过。

服务端 changes 应明确本次固定的截止水位与分页游标，不能一边翻页一边移动快照边界。重复事件可以忽略，但仍算该补拉页已被检查；错误或非法事件不能吞掉后直接推进游标。

**7. 全量同步必须能真正纠正旧库**

现有 `/sync/contacts/snapshot` 保留，新增明确的 `snapshotSeq`，并让所有分页固定在同一个逻辑快照边界 R。每条关系/删除事件保留对应的 `contactSeq`；本地也保存最后接受的变更序号。

全量分页写入临时表，完整校验后才切换：

- 快照 R 中不存在的旧好友，应从已确认名单移除。
- 只保留明确在 R 之后接受的本地版本事件，不能因为旧记录 `itemVersion > 0` 就永久保留。
- R 之后的删除标记必须保护本地删除，不能被 R 时刻的好友快照复活。
- 切换时由同一个同步写入器协调，原子发布快照并保留/重放 R 之后的已确认事件，然后从 R 补拉增量。
- UI pending 操作独立叠加，不混进快照的已确认数据。
- 全量过程中崩溃或分页不完整，保留原完整快照和可恢复进度。

服务端保留增量日志和删除版本到约定的安全窗口；客户端游标过旧时明确返回 SNAPSHOT_REQUIRED。清理删除标记必须配合快照水位和旧事件拒绝规则，不能按任意短 TTL 清理后允许旧新增再次生效。

**8. 对当前客户端的具体改造位置**

| 位置 | 改造方向 |
| --- | --- |
| `lib/src/api/friend_request_api.dart` | 接受成友响应，不再丢弃确认接口的返回变更 |
| `lib/src/api/me_friend_api.dart` | 删除/备注解析版本变更；让迟到关系查询失效 |
| `lib/src/services/friend_local/friend_local_store.dart` | 统一关系写入事务、实际写入结果、版本删除、快照边界及同步游标原子性 |
| `lib/src/services/friend_local/contacts_protocol_sync_service.dart` | 多入口调度、运行中补跑、失败重试、断线恢复与水位校验 |
| `lib/src/services/friend_local/friend_sync_service.dart` | 将当前无版本增删替换为确认事件和 pending 展示；不再默默忽略落库失败 |
| `lib/src/services/friend_realtime/friend_realtime_event.dart` | 解析完整变更元数据；旧协议走触发补拉分支 |
| `lib/src/services/friend_request_notice_service.dart` | 通知统一进入同步服务，不直接低版本增删 |
| `lib/src/services/c2c_friend_message_guard.dart` | 权限查询不再无版本新增/patch 好友关系 |
| `lib/utils/custom_message/friend_became_friends_message.dart` | 成友消息只提示校验，不作为恢复好友的凭证 |
| `third_party/tencent_cloud_chat_uikit/lib/business_logic/view_models/tui_friendship_view_model.dart` | SDK 增删触发校验；通讯录、选择器、搜索统一消费已确认本地名单 |

名单权威切换需要同时调整目录与消费者，不能仅修 SQLite、继续让其他页面按 SDK 名单判断好友。公开资料缓存和在线状态在本次范围内保留，不能借此改造扩大成全部资料重构。

当前 GitNexus 对 ContactsProtocolSyncService 的一层影响结果为 LOW，直接依赖为 home_bootstrap 与 account_session_service，未报告流程。这个结果只覆盖该符号，不代表整项改造低风险；其他函数在实际编辑前须分别分析影响并验证动态调用。

**9. 发布与兼容约定**

先后端新增能力并通过协议测试，再发布支持新协议的客户端。建议新增能力字段 `contactsVersionedMutations: true` 及 `contactsEventSchema: 1`；这里的事件 schema 与已有 snapshot/changes 的 v2 命名分开。通过明确能力字段切换，不能仅凭某次响应恰好带字段就全局切换。

迁移期间服务端可继续兼容旧客户端；新客户端一旦启用版本协议，无版本旧事件只触发同步，不能重新进入旧写入通道。首次切换执行带边界的完整快照，替换旧无版本关系；若既有版本计数与新计数不可比较，应使用明确同步 epoch 和重建流程，不能重置计数后继续比较。

操作幂等结果、日志与删除标记的保留期需明确。若重试超过幂等窗口，应返回可诊断结果并查询现状，不盲目执行旧操作。

**10. 联调验收**

| 场景 | 必须达到的结果 |
| --- | --- |
| 添加新好友、接受申请、自动同意、恢复好友 | HTTP 确认落库后，各入口名单一致 |
| 删除好友且 TCP 推送丢失 | 本机通过 HTTP 结果立即删除，其他设备通过补拉收敛 |
| 删除前关系请求在删除后返回 | 不复活好友，不重新写入旧权限状态 |
| 删除 v18 后到达旧新增 v17 | 维持删除 |
| 删除 v18 后重新添加 v19，再到达重复删除 v18 | 维持好友 |
| 同一事件 HTTP、TCP、changes 到达多次 | 只有一次有效状态变化，不产生重复联系人 |
| 推送乱序、漏中间一条、漏最后一条 | 不跳游标；补拉或水位检查修复 |
| 全量期间发生新增和删除 | 切换快照后保留 R 之后的正确状态 |
| 服务端快照不再包含一个本地旧好友 | 旧记录被清除，不能因旧版本非零而残留 |
| 请求超时但后端已经提交 | 同 mutationId 重试返回原结果，不产生第二次操作 |
| 数据库提交后进程在发推送前崩溃 | 重启后待投递事件重发 |
| 客户端应用事件或分页时崩溃 | 重启允许重放，不跳过未提交事件 |
| 多端同时操作、退出登录后旧请求返回 | 按后端提交顺序收敛，账号之间不串数据 |
| 后端关系正确而 IM SDK 暂时失败 | 本地名单维持后端结果，SDK 投影可重试并可观测 |

日志至少记录 owner、peer、mutationId、eventId、contactSeq、入站/本地 itemVersion、接收来源、applied/ignored 原因和同步游标；不记录完整私有备注。联调应核对后端记录、变更日志、客户端 SQLite 与各页面使用的名单，而不只看刷新动画或 SDK 返回成功。

**交付边界**：当前交付为协议文档和客户端改造范围，没有修改客户端运行逻辑，也没有修改或部署后端。需要服务端实现上述事务与接口后，再进行双端实现、故障注入和联调。
