# 手动回底与回弹来消息：修复记录

本次修复针对 2026-09-22 的两个反馈：手动读完新消息后仍出现“回到底部”；轻微回弹后收到新消息，列表尚有内容在下方却没有提示。修复接入现有生产列表，统一运行时的整体迁移仍未完成。

## 原因与改动

- 胶囊构建和滚动结算各算一次“真实最新端”，其中一处遗漏待入窗消息。现在使用同一个判断，包含内存待入窗、持久待确认和历史缺页。
- 原策略即使有未见消息，也要求离底部半屏才显示。现在尚未到真实最新端且有未见消息时优先显示计数。无新消息的普通回底按钮仍保留距离与滞回规则。
- 恢复跟随只迁移消息 ID，却留下旧 receivedCount。现在完整覆盖、收件代次和待处理数据检查通过后同步收敛，保留访问代次与去重身份；部分覆盖拒绝恢复跟随。
- 可见性采样检查消息行身份与布局有效性。普通消息完整进入视口、超高消息底边进入视口才消费胶囊计数，避免预加载行或边缘露头提前清除提示。
- 有效可见行可以在阅读历史时推进持久已读，无需先恢复跟随。持久 receivedCount 根据实际剩余回执发布，允许下降；胶囊 remainingLiveIncomingIds 独立维护，SQL ACK 本身不清除它。
- 几何到底但持久确认尚未完成时，不再提前跳过确认分支。结算未成功时，两个直接结算入口不会自行清除胶囊状态。

## 验证

在隔离工作树上，先使用 c14939e 原业务代码运行相同八个套件，随后运行修改版；未删除失败测试：

| 检查 | 结果 |
| --- | --- |
| 原有八个套件的 96 项基线 | 64 通过 / 32 失败 |
| 最终十个套件，共 138 项 | 126 通过 / 12 失败 |
| 失败集合对比 | 无新增失败，修复 20 项既有失败 |
| 六个专项页面回归 | 全部通过 |
| live_follow_restore_test.dart | 9 项通过 |
| history_incoming_position_test.dart | 30 项通过 |
| 修改 Dart 文件的分析器检查 | 无 error；保留既有 warning/info |

六个专项页面回归包含浅历史、深历史、补页失败重试下的 95 条消息手动回底，距底 30px 时内存/持久消息到达，以及iOS 滚动物理规则下的越界拖拽、松手回弹期间来消息。两个近底部测试已验证旧业务代码失败、修复后通过。

history_incoming_position_test.dart 的逐条 ACK 测试加强了实际剩余回执数递减断言，同时保留“仅预加载不消费”“失效证明不消费”“重复 ACK 不消费”“胶囊 ID 集合不由 SQL ACK 改动”的检查。两个距离策略测试按本次需求调整为存在未见消息时立即显示。

## 第三批当时未完成的边界

剩余 12 项失败均已在基线失败集合内，涉及部分返回最新端后的迟到消息/超出热缓存的消息、恢复失败后的提示、输入框回底、空闲预加载调度，以及连续 300 条消息跨窗口裁剪。相关断言保留，不能将本次交付描述为所有聊天分支已通过或整体根治。

尚未打包、部署或在真实设备上验收。代码位于 codex/unified-chat-runtime-core 的隔离工作树。

## 可复查证据

日志目录 D:/CodexRuntimeTests/unified-chat-runtime-20260922：

- bottom-before.log：同提交原业务代码基线。
- rebound-red.log：近底部两项失败复现。
- bottom-focused.log：六项专项通过。
- bottom-ledger-regression.log：最终 138 项结果。
- position-before.log：计数发布修改前的位置套件，30 项通过。
- analyze-ledger.log：最终修改文件分析结果。
- bottom-gate-*.json：最终图变更检查。

GitNexus 对可见已读采样的影响为 MEDIUM：五个直接调用点，覆盖列表构建、模型更新、滚动/布局可见性和裁剪；计数发布为 MEDIUM：六个直接调用点，覆盖入站、访问基线、持久合并与 ACK。图未解析到的局部函数和测试入口已核对源码。全局流程枚举有静态分析边界，不能把缺席视为零影响。

## 第四批：剩余 12 项回归修复

本批接续 be41443，修复上述 12 项失败，保留原断言，并加强超出热缓存用例的精确 ID 集合检查。当前十个原套件的 138 项全部通过；加入访问基线与持久未读两个套件后，合计 165 项全部通过。

- 手动阅读只由一个帧后采样器判断已跨过的消息行，同时推进胶囊 ID 集合与持久确认队列。删除另一个独立的同步采样入口，避免窗口裁剪后胶囊残留和空闲状态重复调度。300 条逐行阅读、真实窗口裁剪、像素锚点和静止跑道收敛均通过。
- 主动回底每次尝试在 IO 前捕获消息 ID 集合与访问代次，成功加载有限目标后仅结算该集合。SQL 的 pending-tail 查询最多返回 120 个 ID，不能代替完整的请求快照。121 条后到消息用例同时检查剩余 ID 恰为 103 至 223。
- 回底过程中的后到消息在连接完整的受限窗口继续入窗；结束回底和恢复 FOLLOW 之间，逻辑底部仍允许接入，历史窗口、遮罩、搜索、窗口上限等检查继续生效。
- 自动最新端确认避让显式回底事务，并检查真实消息行布局。显式回底的可见 ACK 检查相同访问代次、列表版本与真实行可见性，成功后只消费该证明内的 ID。迟到消息的零高度行不会被提前确认。
- 点击输入框的主动意图可在键盘动画期间执行一次回底。被动键盘调整和发送回调仍使用原保护条件。

验证记录位于 D:/CodexRuntimeTests/unified-chat-runtime-20260922：

| 检查 | 结果 / 证据 |
| --- | --- |
| 原有 12 项失败 | 全部通过，remaining12-final.log |
| 原十套件 + 访问基线、持久未读 | 165 项通过，remaining12-final.log |
| 超过 120 条后到消息的精确 ID 检查 | 通过，remaining12-focused03.log |
| 修改 Dart 文件分析 | 0 error；41 warning、107 info，remaining12-analyze.log |
| 额外四套件检查 | 74 通过、1 失败，remaining12-ledger.log |
| 额外失败的修改前复验 | 同样失败，baseline-check/baseline.log |

额外失败来自 bounded_history_global_test.dart 的万条入站压力测试，断言 hw_deferred 中保留 120 份正文，实际为 0。raw/hot 缓存上限和持久未读计数断言已经通过，失败发生于正文统计，早于回底 ACK。用 be41443 的原业务文件单独重跑该项得到完全相同结果；本批没有改该存储实现或断言，也没有将扩展检查描述为全绿。

本批修复的是这 12 个生产行为回归，不等于统一运行时已完成全量入口迁移。尚未打包、部署或完成真机验收。
