# 通话气泡：统一结果提交与终态展示

基线：00a1a8a67d69491a98b45c637bc8c635dcbe114e。
分支：codex/call-bubble-state。

## 修复的问题

- 未知 callId 的最近通话/TCP 结果可能先于拨号响应到达。旧逻辑只有本地已知正在通话时才复核 missed/failed，导致提前发布终态，随后振铃又被单调状态合并拒绝。
- 状态接口缺失或不识别 status/phase 时默认 ENDED，凭空生成结束状态。
- 相同会话、相同时长的不同 callId 被弱去重键误合并。
- 通话结果变化时，消息正文不变，元数据和列表缓存仍命中旧结果。
- 已结束的结果缓存会把原邀请/接听消息升级成另一条终态历史消息。

以上五个场景均有修改前失败、修改后通过的测试证据。

## 当前生产链路

拨号、信令、设备结束回调、服务端结果 → CallBubbleInsertService.accept → CallResultRepository 合并 → 读取合并后的记录 → 终态覆盖层及会话预览 → 纯函数生成可见历史。

所有生产 CallResultRepository.save 调用收敛到 accept。身份代次失效的输入不发布。未带明确会话状态、且尚无已确认终态的服务端结果先走状态接口；失败或未知状态不会推断为结束。明确的设备结束和终态信令仍能立即生成气泡，零秒挂断仍保留。

页面只监听结果变化并使展示缓存失效。已移除通话结束后 60ms 刷新、三轮历史重拉、开页/挂载/模型变化的定时去重，以及通过删除/替换正式消息列表清理通话气泡的写入路径。CallBubbleDedupe 保留纯投影；旧调度器及列表写回实现已删除。旧 hold API 仅保留兼容，没有生产调用且不会发布或修改消息。

显式 callId 优先于房间/时长等弱键；同一通话的本地和 IM 记录合并，不同 callId 不因时长相同合并。缓存还依赖账号、代次和结果 revision。邀请、接听等中间态不会因后来缓存的结束结果而变成历史气泡。

## 验证

- 通话、LiveKit、气泡、历史投影与会话预览：218 项通过。
- 覆盖层、清空、归档和清空代次：16 项通过。
- 共 234 项通过；新增 8 项行为回归/提交入口测试。
- 分析器无新增 error/warning；既有提示保留，另有 3 项非功能性风格 info。
- 日志：D:/CodexRuntimeTests/unified-chat-runtime-20260922/call-bubble-*.log、call-invite-cache-before.log、call-history-acceptance.log。

## 范围与兼容

本次统一的是通话结果的生产提交与展示，不等于此前整个聊天运行时和持久事务迁移已经完成。现有按账号保存的通话缓存与数据库格式保留，没有迁走旧版本使用的表。仅含 result、缺少明确会话状态的新记录，需要状态确认才能补入气泡；网络失败时等待后续信令或恢复查询，不展示推测结果。

已做自动测试，尚未完成双机真实语音/视频通话验收。
